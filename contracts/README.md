# BenchProof Intelligent Contract

`benchproof.py` is the canonical V1 contract.

Deploy via GenLayer Studio or `node scripts/deploy-genlayer.mjs`.

Do not add admin mutation methods. Do not fetch arbitrary URLs from evaluation. Keep untrusted evidence inside `<<<UNTRUSTED>>>` delimiters.
